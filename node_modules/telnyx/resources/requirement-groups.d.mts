import { APIResource } from "../core/resource.mjs";
import { APIPromise } from "../core/api-promise.mjs";
import { RequestOptions } from "../internal/request-options.mjs";
/**
 * Requirement Groups
 */
export declare class RequirementGroups extends APIResource {
    /**
     * Returns regulatory requirement groups for the account. Results can be filtered
     * by country, number type, action, approval status, and customer reference.
     *
     * @example
     * ```ts
     * const requirementGroups =
     *   await client.requirementGroups.list();
     * ```
     */
    list(query?: RequirementGroupListParams | null | undefined, options?: RequestOptions): APIPromise<RequirementGroupListResponse>;
    /**
     * Creates a regulatory requirement group for a country, number type, and ordering
     * or porting action. Optional customer-reference and requirement values are
     * retained on the created group.
     *
     * @example
     * ```ts
     * const requirementGroup =
     *   await client.requirementGroups.create({
     *     action: 'ordering',
     *     country_code: 'US',
     *     phone_number_type: 'local',
     *   });
     * ```
     */
    create(body: RequirementGroupCreateParams, options?: RequestOptions): APIPromise<RequirementGroup>;
    /**
     * Deletes the regulatory requirement group identified by `id`. The response
     * contains the deleted requirement-group representation.
     *
     * @example
     * ```ts
     * const requirementGroup =
     *   await client.requirementGroups.delete('id');
     * ```
     */
    delete(id: string, options?: RequestOptions): APIPromise<RequirementGroup>;
    /**
     * Returns the regulatory requirement group identified by `id`, including its
     * requirement values and current approval status.
     *
     * @example
     * ```ts
     * const requirementGroup =
     *   await client.requirementGroups.retrieve('id');
     * ```
     */
    retrieve(id: string, options?: RequestOptions): APIPromise<RequirementGroup>;
    /**
     * Updates the customer reference or regulatory requirement values on the specified
     * requirement group. The response contains the updated group.
     *
     * @example
     * ```ts
     * const requirementGroup =
     *   await client.requirementGroups.update('id');
     * ```
     */
    update(id: string, body: RequirementGroupUpdateParams, options?: RequestOptions): APIPromise<RequirementGroup>;
    /**
     * Submits the specified regulatory requirement group for approval. The response
     * contains the requirement group with its resulting approval status.
     *
     * @example
     * ```ts
     * const requirementGroup =
     *   await client.requirementGroups.submitForApproval('id');
     * ```
     */
    submitForApproval(id: string, options?: RequestOptions): APIPromise<RequirementGroup>;
}
export interface RequirementGroup {
    id?: string;
    action?: string;
    country_code?: string;
    created_at?: string;
    customer_reference?: string;
    phone_number_type?: string;
    record_type?: string;
    regulatory_requirements?: Array<UserRequirement>;
    status?: 'approved' | 'unapproved' | 'pending-approval' | 'declined' | 'expired';
    updated_at?: string;
}
export interface UserRequirement {
    created_at?: string;
    expires_at?: string;
    field_type?: string;
    field_value?: string;
    requirement_id?: string;
    status?: 'approved' | 'unapproved' | 'pending-approval' | 'declined' | 'expired';
    updated_at?: string;
}
export type RequirementGroupListResponse = Array<RequirementGroup>;
export interface RequirementGroupListParams {
    /**
     * Consolidated filter parameter (deepObject style). Originally:
     * filter[country_code], filter[phone_number_type], filter[action], filter[status],
     * filter[customer_reference]
     */
    filter?: RequirementGroupListParams.Filter;
}
export declare namespace RequirementGroupListParams {
    /**
     * Consolidated filter parameter (deepObject style). Originally:
     * filter[country_code], filter[phone_number_type], filter[action], filter[status],
     * filter[customer_reference]
     */
    interface Filter {
        /**
         * Filter requirement groups by action type
         */
        action?: 'ordering' | 'porting' | 'action';
        /**
         * Filter requirement groups by country code (iso alpha 2)
         */
        country_code?: string;
        /**
         * Filter requirement groups by customer reference
         */
        customer_reference?: string;
        /**
         * Filter requirement groups by phone number type.
         */
        phone_number_type?: 'local' | 'toll_free' | 'mobile' | 'national' | 'shared_cost';
        /**
         * Filter requirement groups by status
         */
        status?: 'approved' | 'unapproved' | 'pending-approval' | 'declined' | 'expired';
    }
}
export interface RequirementGroupCreateParams {
    action: 'ordering' | 'porting';
    /**
     * ISO alpha 2 country code
     */
    country_code: string;
    phone_number_type: 'local' | 'toll_free' | 'mobile' | 'national' | 'shared_cost';
    customer_reference?: string;
    regulatory_requirements?: Array<RequirementGroupCreateParams.RegulatoryRequirement>;
}
export declare namespace RequirementGroupCreateParams {
    interface RegulatoryRequirement {
        field_value?: string;
        requirement_id?: string;
    }
}
export interface RequirementGroupUpdateParams {
    /**
     * Reference for the customer
     */
    customer_reference?: string;
    regulatory_requirements?: Array<RequirementGroupUpdateParams.RegulatoryRequirement>;
}
export declare namespace RequirementGroupUpdateParams {
    interface RegulatoryRequirement {
        /**
         * New value for the regulatory requirement
         */
        field_value?: string;
        /**
         * Unique identifier for the regulatory requirement
         */
        requirement_id?: string;
    }
}
export declare namespace RequirementGroups {
    export { type RequirementGroup as RequirementGroup, type UserRequirement as UserRequirement, type RequirementGroupListResponse as RequirementGroupListResponse, type RequirementGroupListParams as RequirementGroupListParams, type RequirementGroupCreateParams as RequirementGroupCreateParams, type RequirementGroupUpdateParams as RequirementGroupUpdateParams, };
}
//# sourceMappingURL=requirement-groups.d.mts.map