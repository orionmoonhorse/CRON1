// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import { DefaultFlatPagination } from "../../core/pagination.mjs";
/**
 * Manage Whatsapp message templates
 */
export class Templates extends APIResource {
    /**
     * Returns WhatsApp message templates owned by the authenticated account, including
     * their current review state.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const whatsappTemplateData of client.whatsapp.templates.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/v2/whatsapp/message_templates', (DefaultFlatPagination), { query, ...options });
    }
    /**
     * Creates a WhatsApp message template for review and subsequent use in template
     * messages.
     *
     * @example
     * ```ts
     * const template = await client.whatsapp.templates.create({
     *   category: 'MARKETING',
     *   components: [{ format: 'TEXT', type: 'HEADER' }],
     *   language: 'language',
     *   name: 'name',
     *   waba_id: 'waba_id',
     * });
     * ```
     */
    create(body, options) {
        return this._client.post('/v2/whatsapp/message_templates', { body, ...options });
    }
}
//# sourceMappingURL=templates.mjs.map