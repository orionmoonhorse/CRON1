// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../../core/resource.mjs";
import { DefaultPaginationForQueues, } from "../../../core/pagination.mjs";
import { buildHeaders } from "../../../internal/headers.mjs";
import { path } from "../../../internal/utils/path.mjs";
/**
 * TeXML REST Commands
 */
export class Queues extends APIResource {
    /**
     * Creates a new queue resource for the account with the provided settings and
     * returns it.
     *
     * @example
     * ```ts
     * const queueResource =
     *   await client.texml.accounts.queues.create('account_sid');
     * ```
     */
    create(accountSid, body, options) {
        return this._client.post(path `/texml/Accounts/${accountSid}/Queues`, {
            body,
            ...options,
            headers: buildHeaders([{ 'Content-Type': 'application/x-www-form-urlencoded' }, options?.headers]),
        });
    }
    /**
     * Permanently deletes the specified queue resource from the account.
     *
     * @example
     * ```ts
     * await client.texml.accounts.queues.delete('queue_sid', {
     *   account_sid: 'account_sid',
     * });
     * ```
     */
    delete(queueSid, params, options) {
        const { account_sid } = params;
        return this._client.delete(path `/texml/Accounts/${account_sid}/Queues/${queueSid}`, {
            ...options,
            headers: buildHeaders([{ Accept: '*/*' }, options?.headers]),
        });
    }
    /**
     * Returns a single queue resource for the account by its QueueSid.
     *
     * @example
     * ```ts
     * const queueResource =
     *   await client.texml.accounts.queues.retrieve('queue_sid', {
     *     account_sid: 'account_sid',
     *   });
     * ```
     */
    retrieve(queueSid, params, options) {
        const { account_sid } = params;
        return this._client.get(path `/texml/Accounts/${account_sid}/Queues/${queueSid}`, options);
    }
    /**
     * Updates the specified queue resource's settings and returns the updated queue.
     *
     * @example
     * ```ts
     * const queueResource =
     *   await client.texml.accounts.queues.update('queue_sid', {
     *     account_sid: 'account_sid',
     *   });
     * ```
     */
    update(queueSid, params, options) {
        const { account_sid, ...body } = params;
        return this._client.post(path `/texml/Accounts/${account_sid}/Queues/${queueSid}`, {
            body,
            ...options,
            headers: buildHeaders([{ 'Content-Type': 'application/x-www-form-urlencoded' }, options?.headers]),
        });
    }
    /**
     * Returns a paginated list of queue resources for the account, with support for
     * filtering by creation or update dates.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const queueResource of client.texml.accounts.queues.list(
     *   'account_sid',
     * )) {
     *   // ...
     * }
     * ```
     */
    list(accountSid, query = {}, options) {
        return this._client.getAPIList(path `/texml/Accounts/${accountSid}/Queues`, (DefaultPaginationForQueues), { query, ...options });
    }
}
//# sourceMappingURL=queues.mjs.map