// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../../../core/resource.mjs";
import { PerPagePagination } from "../../../../core/pagination.mjs";
import { buildHeaders } from "../../../../internal/headers.mjs";
import { path } from "../../../../internal/utils/path.mjs";
/**
 * Number lookup usage reports
 */
export class NumberLookup extends APIResource {
    /**
     * Retrieve a paginated list of telco data usage reports
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const telcoDataUsageReportResponse of client.legacy.reporting.usageReports.numberLookup.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/legacy/reporting/usage_reports/number_lookup', (PerPagePagination), { query, ...options });
    }
    /**
     * Submits a new telco data (number lookup) usage report request. The report is
     * generated asynchronously; retrieve it by its identifier once ready.
     *
     * @example
     * ```ts
     * const numberLookup =
     *   await client.legacy.reporting.usageReports.numberLookup.create();
     * ```
     */
    create(body, options) {
        return this._client.post('/legacy/reporting/usage_reports/number_lookup', {
            body,
            ...options,
            headers: buildHeaders([{ 'Content-Type': '*/*' }, options?.headers]),
        });
    }
    /**
     * Delete a specific telco data usage report by its ID
     *
     * @example
     * ```ts
     * await client.legacy.reporting.usageReports.numberLookup.delete(
     *   'id',
     * );
     * ```
     */
    delete(id, options) {
        return this._client.delete(path `/legacy/reporting/usage_reports/number_lookup/${id}`, {
            ...options,
            headers: buildHeaders([{ Accept: '*/*' }, options?.headers]),
        });
    }
    /**
     * Retrieve a specific telco data usage report by its ID
     *
     * @example
     * ```ts
     * const numberLookup =
     *   await client.legacy.reporting.usageReports.numberLookup.retrieve(
     *     'id',
     *   );
     * ```
     */
    retrieve(id, options) {
        return this._client.get(path `/legacy/reporting/usage_reports/number_lookup/${id}`, options);
    }
}
//# sourceMappingURL=number-lookup.mjs.map