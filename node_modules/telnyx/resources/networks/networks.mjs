// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import * as DefaultGatewayAPI from "./default-gateway.mjs";
import { DefaultGatewayResource, } from "./default-gateway.mjs";
import { DefaultFlatPagination } from "../../core/pagination.mjs";
import { path } from "../../internal/utils/path.mjs";
/**
 * Network operations
 */
export class Networks extends APIResource {
    constructor() {
        super(...arguments);
        this.defaultGateway = new DefaultGatewayAPI.DefaultGatewayResource(this._client);
    }
    /**
     * Returns a paginated list of the private networks on your account, with support
     * for filtering.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const network of client.networks.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/networks', (DefaultFlatPagination), { query, ...options });
    }
    /**
     * Creates a new private network, the container that links your WireGuard
     * interfaces, gateways, and cross connects.
     *
     * @example
     * ```ts
     * const network = await client.networks.create({
     *   network_create: {},
     * });
     * ```
     */
    create(params, options) {
        const { network_create } = params;
        return this._client.post('/networks', { body: network_create, ...options });
    }
    /**
     * Permanently deletes the specified network from your account.
     *
     * @example
     * ```ts
     * const network = await client.networks.delete(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    delete(id, options) {
        return this._client.delete(path `/networks/${id}`, options);
    }
    /**
     * Returns the details of a single network by its identifier.
     *
     * @example
     * ```ts
     * const network = await client.networks.retrieve(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    retrieve(id, options) {
        return this._client.get(path `/networks/${id}`, options);
    }
    /**
     * Updates the specified network's attributes and returns the updated network.
     *
     * @example
     * ```ts
     * const network = await client.networks.update(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     *   { network_create: {} },
     * );
     * ```
     */
    update(networkID, params, options) {
        const { network_create } = params;
        return this._client.patch(path `/networks/${networkID}`, { body: network_create, ...options });
    }
    /**
     * Returns a paginated list of the interfaces attached to the specified network,
     * with support for filtering.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const networkListInterfacesResponse of client.networks.listInterfaces(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * )) {
     *   // ...
     * }
     * ```
     */
    listInterfaces(id, query = {}, options) {
        return this._client.getAPIList(path `/networks/${id}/network_interfaces`, (DefaultFlatPagination), { query, ...options });
    }
}
Networks.DefaultGatewayResource = DefaultGatewayResource;
//# sourceMappingURL=networks.mjs.map