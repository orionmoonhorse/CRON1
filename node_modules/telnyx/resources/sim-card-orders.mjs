// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../core/resource.mjs";
import { DefaultFlatPagination } from "../core/pagination.mjs";
import { path } from "../internal/utils/path.mjs";
/**
 * SIM Card Orders operations
 */
export class SimCardOrders extends APIResource {
    /**
     * Get all SIM card orders according to filters.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const simCardOrder of client.simCardOrders.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/sim_card_orders', (DefaultFlatPagination), {
            query,
            ...options,
        });
    }
    /**
     * Creates a new order for physical SIM cards, including quantity and shipping
     * details, and returns the created order.
     *
     * @example
     * ```ts
     * const simCardOrder = await client.simCardOrders.create({
     *   address_id: '1293384261075731499',
     *   quantity: 23,
     * });
     * ```
     */
    create(body, options) {
        return this._client.post('/sim_card_orders', { body, ...options });
    }
    /**
     * Returns the details of a single SIM card order by its ID, including its status.
     *
     * @example
     * ```ts
     * const simCardOrder = await client.simCardOrders.retrieve(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    retrieve(id, options) {
        return this._client.get(path `/sim_card_orders/${id}`, options);
    }
}
//# sourceMappingURL=sim-card-orders.mjs.map