// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../core/resource.mjs";
import { DefaultFlatPagination } from "../core/pagination.mjs";
import { path } from "../internal/utils/path.mjs";
export class NumberBlockOrders extends APIResource {
    /**
     * Get a paginated list of number block orders.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const numberBlockOrder of client.numberBlockOrders.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/number_block_orders', (DefaultFlatPagination), {
            query,
            ...options,
        });
    }
    /**
     * Creates an order for a block of consecutive phone numbers and returns the
     * created order. Track fulfillment through the order's status.
     *
     * @example
     * ```ts
     * const numberBlockOrder =
     *   await client.numberBlockOrders.create({
     *     range: 10,
     *     starting_number: '+19705555000',
     *   });
     * ```
     */
    create(body, options) {
        return this._client.post('/number_block_orders', { body, ...options });
    }
    /**
     * Get an existing phone number block order.
     *
     * @example
     * ```ts
     * const numberBlockOrder =
     *   await client.numberBlockOrders.retrieve(
     *     'number_block_order_id',
     *   );
     * ```
     */
    retrieve(numberBlockOrderID, options) {
        return this._client.get(path `/number_block_orders/${numberBlockOrderID}`, options);
    }
}
//# sourceMappingURL=number-block-orders.mjs.map