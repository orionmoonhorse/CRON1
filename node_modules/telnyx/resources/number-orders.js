"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.NumberOrders = void 0;
const resource_1 = require("../core/resource.js");
const pagination_1 = require("../core/pagination.js");
const path_1 = require("../internal/utils/path.js");
/**
 * Number orders
 */
class NumberOrders extends resource_1.APIResource {
    /**
     * Returns a paginated list of your phone number orders, with support for
     * filtering.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const numberOrderListResponse of client.numberOrders.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/number_orders', (pagination_1.DefaultFlatPagination), {
            query,
            ...options,
        });
    }
    /**
     * Creates an order to purchase the specified phone numbers and returns the created
     * order. Track fulfillment through the order's status.
     *
     * @example
     * ```ts
     * const numberOrder = await client.numberOrders.create();
     * ```
     */
    create(body, options) {
        return this._client.post('/number_orders', { body, ...options });
    }
    /**
     * Returns the details of an existing phone number order, including its status and
     * the numbers included.
     *
     * @example
     * ```ts
     * const numberOrder = await client.numberOrders.retrieve(
     *   'number_order_id',
     * );
     * ```
     */
    retrieve(numberOrderID, options) {
        return this._client.get((0, path_1.path) `/number_orders/${numberOrderID}`, options);
    }
    /**
     * Updates an existing phone number order, for example to satisfy regulatory
     * requirements attached to the order, and returns the updated order.
     *
     * @example
     * ```ts
     * const numberOrder = await client.numberOrders.update(
     *   'number_order_id',
     * );
     * ```
     */
    update(numberOrderID, body, options) {
        return this._client.patch((0, path_1.path) `/number_orders/${numberOrderID}`, { body, ...options });
    }
}
exports.NumberOrders = NumberOrders;
//# sourceMappingURL=number-orders.js.map