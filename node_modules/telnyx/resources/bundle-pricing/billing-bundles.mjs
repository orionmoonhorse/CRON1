// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import { DefaultFlatPagination } from "../../core/pagination.mjs";
import { buildHeaders } from "../../internal/headers.mjs";
import { path } from "../../internal/utils/path.mjs";
export class BillingBundles extends APIResource {
    /**
     * Returns a paginated list of the billing bundles available to your account, with
     * support for filtering.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const billingBundleSummary of client.bundlePricing.billingBundles.list()) {
     *   // ...
     * }
     * ```
     */
    list(params = {}, options) {
        const { authorization_bearer, ...query } = params ?? {};
        return this._client.getAPIList('/bundle_pricing/billing_bundles', (DefaultFlatPagination), {
            query,
            ...options,
            headers: buildHeaders([
                { ...(authorization_bearer != null ? { authorization_bearer: authorization_bearer } : undefined) },
                options?.headers,
            ]),
        });
    }
    /**
     * Returns the details of a single billing bundle by its ID, so you can inspect its
     * contents before purchasing a user bundle.
     *
     * @example
     * ```ts
     * const billingBundle =
     *   await client.bundlePricing.billingBundles.retrieve(
     *     '8661948c-a386-4385-837f-af00f40f111a',
     *   );
     * ```
     */
    retrieve(bundleID, params = {}, options) {
        const { authorization_bearer } = params ?? {};
        return this._client.get(path `/bundle_pricing/billing_bundles/${bundleID}`, {
            ...options,
            headers: buildHeaders([
                { ...(authorization_bearer != null ? { authorization_bearer: authorization_bearer } : undefined) },
                options?.headers,
            ]),
        });
    }
}
//# sourceMappingURL=billing-bundles.mjs.map