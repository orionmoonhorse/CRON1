import { APIResource } from "../core/resource.mjs";
import * as WireguardPeersAPI from "./wireguard-peers.mjs";
import * as GlobalIPAssignmentsAPI from "./global-ip-assignments.mjs";
import { APIPromise } from "../core/api-promise.mjs";
import { DefaultFlatPagination, type DefaultFlatPaginationParams, PagePromise } from "../core/pagination.mjs";
import { RequestOptions } from "../internal/request-options.mjs";
/**
 * WireGuard Interface operations
 */
export declare class WireguardPeers extends APIResource {
    /**
     * Returns a paginated list of your WireGuard peers, with support for filtering.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const wireguardPeer of client.wireguardPeers.list()) {
     *   // ...
     * }
     * ```
     */
    list(query?: WireguardPeerListParams | null | undefined, options?: RequestOptions): PagePromise<WireguardPeersDefaultFlatPagination, WireguardPeer>;
    /**
     * Create a new WireGuard Peer. Current limitation of 5 peers per interface can be
     * created.
     *
     * @example
     * ```ts
     * const wireguardPeer = await client.wireguardPeers.create({
     *   body: {},
     * });
     * ```
     */
    create(params: WireguardPeerCreateParams, options?: RequestOptions): APIPromise<WireguardPeerCreateResponse>;
    /**
     * Deletes the specified WireGuard peer from its interface.
     *
     * @example
     * ```ts
     * const wireguardPeer = await client.wireguardPeers.delete(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    delete(id: string, options?: RequestOptions): APIPromise<WireguardPeerDeleteResponse>;
    /**
     * Returns the details of a single WireGuard peer by its identifier.
     *
     * @example
     * ```ts
     * const wireguardPeer = await client.wireguardPeers.retrieve(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    retrieve(id: string, options?: RequestOptions): APIPromise<WireguardPeerRetrieveResponse>;
    /**
     * Updates the specified WireGuard peer and returns the updated peer.
     *
     * @example
     * ```ts
     * const wireguardPeer = await client.wireguardPeers.update(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    update(id: string, body: WireguardPeerUpdateParams, options?: RequestOptions): APIPromise<WireguardPeerUpdateResponse>;
    /**
     * Retrieve Wireguard config template for Peer
     *
     * @example
     * ```ts
     * const response = await client.wireguardPeers.retrieveConfig(
     *   '6a09cdc3-8948-47f0-aa62-74ac943d6c58',
     * );
     * ```
     */
    retrieveConfig(id: string, options?: RequestOptions): APIPromise<string>;
}
export type WireguardPeersDefaultFlatPagination = DefaultFlatPagination<WireguardPeer>;
export interface WireguardPeer extends GlobalIPAssignmentsAPI.Record {
    /**
     * ISO 8601 formatted date-time indicating when peer sent traffic last time.
     */
    last_seen?: string;
    /**
     * Your WireGuard `Interface.PrivateKey`.<br /><br />This attribute is only ever
     * utlised if, on POST, you do NOT provide your own `public_key`. In which case, a
     * new Public and Private key pair will be generated for you. When your
     * `private_key` is returned, you must save this immediately as we do not save it
     * within Telnyx. If you lose your Private Key, it can not be recovered.
     */
    private_key?: string;
    /**
     * The id of the wireguard interface associated with the peer.
     */
    wireguard_interface_id?: string;
}
export interface WireguardPeerPatch {
    /**
     * The WireGuard `PublicKey`.<br /><br />If you do not provide a Public Key, a new
     * Public and Private key pair will be generated for you.
     */
    public_key?: string;
}
export interface WireguardPeerCreateResponse {
    data?: WireguardPeer;
}
export interface WireguardPeerRetrieveResponse {
    data?: WireguardPeer;
}
export interface WireguardPeerUpdateResponse {
    data?: WireguardPeer;
}
export interface WireguardPeerDeleteResponse {
    data?: WireguardPeer;
}
export type WireguardPeerRetrieveConfigResponse = string;
export interface WireguardPeerListParams extends DefaultFlatPaginationParams {
    /**
     * Consolidated filter parameter (deepObject style). Originally:
     * filter[wireguard_interface_id]
     */
    filter?: WireguardPeerListParams.Filter;
}
export declare namespace WireguardPeerListParams {
    /**
     * Consolidated filter parameter (deepObject style). Originally:
     * filter[wireguard_interface_id]
     */
    interface Filter {
        /**
         * The id of the associated WireGuard interface to filter on.
         */
        wireguard_interface_id?: string;
    }
}
export interface WireguardPeerCreateParams {
    body: WireguardPeerCreateParams.Body;
}
export declare namespace WireguardPeerCreateParams {
    interface Body extends WireguardPeersAPI.WireguardPeer {
    }
}
export interface WireguardPeerUpdateParams {
    /**
     * The WireGuard `PublicKey`.<br /><br />If you do not provide a Public Key, a new
     * Public and Private key pair will be generated for you.
     */
    public_key?: string;
}
export declare namespace WireguardPeers {
    export { type WireguardPeer as WireguardPeer, type WireguardPeerPatch as WireguardPeerPatch, type WireguardPeerCreateResponse as WireguardPeerCreateResponse, type WireguardPeerRetrieveResponse as WireguardPeerRetrieveResponse, type WireguardPeerUpdateResponse as WireguardPeerUpdateResponse, type WireguardPeerDeleteResponse as WireguardPeerDeleteResponse, type WireguardPeerRetrieveConfigResponse as WireguardPeerRetrieveConfigResponse, type WireguardPeersDefaultFlatPagination as WireguardPeersDefaultFlatPagination, type WireguardPeerListParams as WireguardPeerListParams, type WireguardPeerCreateParams as WireguardPeerCreateParams, type WireguardPeerUpdateParams as WireguardPeerUpdateParams, };
}
//# sourceMappingURL=wireguard-peers.d.mts.map