// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../core/resource.mjs";
import { DefaultFlatPagination } from "../core/pagination.mjs";
import { path } from "../internal/utils/path.mjs";
/**
 * Notification settings operations
 */
export class NotificationChannels extends APIResource {
    /**
     * Returns a paginated list of your notification channels, the destinations that
     * receive notifications.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const notificationChannel of client.notificationChannels.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/notification_channels', (DefaultFlatPagination), {
            query,
            ...options,
        });
    }
    /**
     * Creates a new notification channel defining where notifications are delivered,
     * and returns the created channel.
     *
     * @example
     * ```ts
     * const notificationChannel =
     *   await client.notificationChannels.create();
     * ```
     */
    create(body = {}, options) {
        return this._client.post('/notification_channels', { body, ...options });
    }
    /**
     * Deletes the specified notification channel so notifications are no longer
     * delivered to it.
     *
     * @example
     * ```ts
     * const notificationChannel =
     *   await client.notificationChannels.delete(
     *     '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     *   );
     * ```
     */
    delete(id, options) {
        return this._client.delete(path `/notification_channels/${id}`, options);
    }
    /**
     * Returns the details of a single notification channel by its identifier.
     *
     * @example
     * ```ts
     * const notificationChannel =
     *   await client.notificationChannels.retrieve(
     *     '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     *   );
     * ```
     */
    retrieve(id, options) {
        return this._client.get(path `/notification_channels/${id}`, options);
    }
    /**
     * Updates the specified notification channel and returns the updated channel.
     *
     * @example
     * ```ts
     * const notificationChannel =
     *   await client.notificationChannels.update(
     *     '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     *   );
     * ```
     */
    update(notificationChannelID, body, options) {
        return this._client.patch(path `/notification_channels/${notificationChannelID}`, { body, ...options });
    }
}
//# sourceMappingURL=notification-channels.mjs.map