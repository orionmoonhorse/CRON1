import { APIResource } from "../../../core/resource.js";
import { APIPromise } from "../../../core/api-promise.js";
import { RequestOptions } from "../../../internal/request-options.js";
export declare class Tools extends APIResource {
    /**
     * Returns the tools configured on the specified mission. Tools define the actions
     * agents may invoke while executing the mission's runs.
     *
     * @example
     * ```ts
     * const response = await client.ai.missions.tools.listTools(
     *   'mission_id',
     * );
     * ```
     */
    listTools(missionID: string, options?: RequestOptions): APIPromise<unknown>;
    /**
     * Adds a new tool to the specified mission, defining an action agents can invoke
     * during runs of this mission.
     *
     * @example
     * ```ts
     * const response = await client.ai.missions.tools.createTool(
     *   'mission_id',
     * );
     * ```
     */
    createTool(missionID: string, options?: RequestOptions): APIPromise<unknown>;
    /**
     * Removes the specified tool from the mission so agents can no longer invoke it in
     * subsequent runs.
     *
     * @example
     * ```ts
     * await client.ai.missions.tools.deleteTool('tool_id', {
     *   mission_id: 'mission_id',
     * });
     * ```
     */
    deleteTool(toolID: string, params: ToolDeleteToolParams, options?: RequestOptions): APIPromise<void>;
    /**
     * Returns the definition of a single tool configured on the specified mission.
     *
     * @example
     * ```ts
     * const response = await client.ai.missions.tools.getTool(
     *   'tool_id',
     *   { mission_id: 'mission_id' },
     * );
     * ```
     */
    getTool(toolID: string, params: ToolGetToolParams, options?: RequestOptions): APIPromise<unknown>;
    /**
     * Replaces the definition of the specified tool on this mission.
     *
     * @example
     * ```ts
     * const response = await client.ai.missions.tools.updateTool(
     *   'tool_id',
     *   { mission_id: 'mission_id' },
     * );
     * ```
     */
    updateTool(toolID: string, params: ToolUpdateToolParams, options?: RequestOptions): APIPromise<unknown>;
}
export type ToolCreateToolResponse = unknown;
export type ToolGetToolResponse = unknown;
export type ToolListToolsResponse = unknown;
export type ToolUpdateToolResponse = unknown;
export interface ToolDeleteToolParams {
    /**
     * Unique identifier of the mission.
     */
    mission_id: string;
}
export interface ToolGetToolParams {
    /**
     * Unique identifier of the mission.
     */
    mission_id: string;
}
export interface ToolUpdateToolParams {
    /**
     * Unique identifier of the mission.
     */
    mission_id: string;
}
export declare namespace Tools {
    export { type ToolCreateToolResponse as ToolCreateToolResponse, type ToolGetToolResponse as ToolGetToolResponse, type ToolListToolsResponse as ToolListToolsResponse, type ToolUpdateToolResponse as ToolUpdateToolResponse, type ToolDeleteToolParams as ToolDeleteToolParams, type ToolGetToolParams as ToolGetToolParams, type ToolUpdateToolParams as ToolUpdateToolParams, };
}
//# sourceMappingURL=tools.d.ts.map