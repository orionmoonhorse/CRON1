"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.McpServers = void 0;
const resource_1 = require("../../../core/resource.js");
const headers_1 = require("../../../internal/headers.js");
const path_1 = require("../../../internal/utils/path.js");
class McpServers extends resource_1.APIResource {
    /**
     * Returns the MCP servers configured on the specified mission. MCP servers expose
     * external tools and data sources agents can use during runs.
     *
     * @example
     * ```ts
     * const response =
     *   await client.ai.missions.mcpServers.listMcpServers(
     *     'mission_id',
     *   );
     * ```
     */
    listMcpServers(missionID, options) {
        return this._client.get((0, path_1.path) `/ai/missions/${missionID}/mcp-servers`, options);
    }
    /**
     * Adds an MCP server to the specified mission, making the server's tools available
     * to agents during runs of this mission.
     *
     * @example
     * ```ts
     * const response =
     *   await client.ai.missions.mcpServers.createMcpServer(
     *     'mission_id',
     *   );
     * ```
     */
    createMcpServer(missionID, options) {
        return this._client.post((0, path_1.path) `/ai/missions/${missionID}/mcp-servers`, options);
    }
    /**
     * Removes the specified MCP server from the mission, revoking agent access to its
     * tools in subsequent runs.
     *
     * @example
     * ```ts
     * await client.ai.missions.mcpServers.deleteMcpServer(
     *   'mcp_server_id',
     *   { mission_id: 'mission_id' },
     * );
     * ```
     */
    deleteMcpServer(mcpServerID, params, options) {
        const { mission_id } = params;
        return this._client.delete((0, path_1.path) `/ai/missions/${mission_id}/mcp-servers/${mcpServerID}`, {
            ...options,
            headers: (0, headers_1.buildHeaders)([{ Accept: '*/*' }, options?.headers]),
        });
    }
    /**
     * Returns the configuration of a single MCP server attached to the specified
     * mission.
     *
     * @example
     * ```ts
     * const response =
     *   await client.ai.missions.mcpServers.getMcpServer(
     *     'mcp_server_id',
     *     { mission_id: 'mission_id' },
     *   );
     * ```
     */
    getMcpServer(mcpServerID, params, options) {
        const { mission_id } = params;
        return this._client.get((0, path_1.path) `/ai/missions/${mission_id}/mcp-servers/${mcpServerID}`, options);
    }
    /**
     * Replaces the configuration of the specified MCP server on this mission.
     *
     * @example
     * ```ts
     * const response =
     *   await client.ai.missions.mcpServers.updateMcpServer(
     *     'mcp_server_id',
     *     { mission_id: 'mission_id' },
     *   );
     * ```
     */
    updateMcpServer(mcpServerID, params, options) {
        const { mission_id } = params;
        return this._client.put((0, path_1.path) `/ai/missions/${mission_id}/mcp-servers/${mcpServerID}`, options);
    }
}
exports.McpServers = McpServers;
//# sourceMappingURL=mcp-servers.js.map