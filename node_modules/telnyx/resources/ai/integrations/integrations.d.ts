import { APIResource } from "../../../core/resource.js";
import * as ConnectionsAPI from "./connections.js";
import { ConnectionListResponse, ConnectionRetrieveResponse, Connections, IntegrationConnection } from "./connections.js";
import { APIPromise } from "../../../core/api-promise.js";
import { RequestOptions } from "../../../internal/request-options.js";
export declare class Integrations extends APIResource {
    connections: ConnectionsAPI.Connections;
    /**
     * Returns the list of third-party integrations available to connect to your AI
     * assistants and workflows.
     *
     * @example
     * ```ts
     * const integrations = await client.ai.integrations.list();
     * ```
     */
    list(options?: RequestOptions): APIPromise<IntegrationListResponse>;
    /**
     * Returns the details of a single available integration, including its
     * configuration details.
     *
     * @example
     * ```ts
     * const integration = await client.ai.integrations.retrieve(
     *   'integration_id',
     * );
     * ```
     */
    retrieve(integrationID: string, options?: RequestOptions): APIPromise<Integration>;
}
export interface Integration {
    id: string;
    available_tools: Array<string>;
    description: string;
    display_name: string;
    logo_url: string;
    name: string;
    status: 'disconnected' | 'connected';
}
export interface IntegrationListResponse {
    data: Array<Integration>;
}
export declare namespace Integrations {
    export { type Integration as Integration, type IntegrationListResponse as IntegrationListResponse };
    export { Connections as Connections, type IntegrationConnection as IntegrationConnection, type ConnectionRetrieveResponse as ConnectionRetrieveResponse, type ConnectionListResponse as ConnectionListResponse, };
}
//# sourceMappingURL=integrations.d.ts.map