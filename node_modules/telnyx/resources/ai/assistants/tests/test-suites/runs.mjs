// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../../../../core/resource.mjs";
import { DefaultFlatPagination, } from "../../../../../core/pagination.mjs";
import { buildHeaders } from "../../../../../internal/headers.mjs";
import { path } from "../../../../../internal/utils/path.mjs";
/**
 * Configure AI assistant specifications
 */
export class Runs extends APIResource {
    /**
     * Retrieves paginated history of test runs for a specific test suite with
     * filtering options
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const testRunResponse of client.ai.assistants.tests.testSuites.runs.list(
     *   'suite_name',
     * )) {
     *   // ...
     * }
     * ```
     */
    list(suiteName, query = {}, options) {
        return this._client.getAPIList(path `/ai/assistants/tests/test-suites/${suiteName}/runs`, (DefaultFlatPagination), { query, ...options });
    }
    /**
     * Executes all tests within a specific test suite as a batch operation
     *
     * @example
     * ```ts
     * const testRunResponses =
     *   await client.ai.assistants.tests.testSuites.runs.trigger(
     *     'suite_name',
     *   );
     * ```
     */
    trigger(suiteName, params = {}, options) {
        const { 'Idempotency-Key': idempotencyKey, ...body } = params ?? {};
        return this._client.post(path `/ai/assistants/tests/test-suites/${suiteName}/runs`, {
            body,
            ...options,
            headers: buildHeaders([
                { ...(idempotencyKey != null ? { 'Idempotency-Key': idempotencyKey } : undefined) },
                options?.headers,
            ]),
        });
    }
}
//# sourceMappingURL=runs.mjs.map