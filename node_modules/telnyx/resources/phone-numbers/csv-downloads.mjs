// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import { DefaultFlatPagination } from "../../core/pagination.mjs";
import { path } from "../../internal/utils/path.mjs";
export class CsvDownloads extends APIResource {
    /**
     * Returns CSV export jobs created for account phone numbers, including each
     * export's current status and pagination metadata.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const csvDownload of client.phoneNumbers.csvDownloads.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/phone_numbers/csv_downloads', (DefaultFlatPagination), {
            query,
            ...options,
        });
    }
    /**
     * Starts generation of a CSV export for phone numbers matching the supplied
     * filters. The `csv_format` parameter selects the output format, and the response
     * contains the resulting download record.
     *
     * @example
     * ```ts
     * const csvDownload =
     *   await client.phoneNumbers.csvDownloads.create();
     * ```
     */
    create(params = {}, options) {
        const { csv_format, filter } = params ?? {};
        return this._client.post('/phone_numbers/csv_downloads', { query: { csv_format, filter }, ...options });
    }
    /**
     * Returns the current status and download details for the CSV export identified by
     * `id`.
     *
     * @example
     * ```ts
     * const csvDownload =
     *   await client.phoneNumbers.csvDownloads.retrieve('id');
     * ```
     */
    retrieve(id, options) {
        return this._client.get(path `/phone_numbers/csv_downloads/${id}`, options);
    }
}
//# sourceMappingURL=csv-downloads.mjs.map