import { APIResource } from "../../core/resource.mjs";
import * as SuppressionsAPI from "./suppressions.mjs";
import { SuppressionCreateParams, SuppressionDeleteParams, SuppressionListParams, Suppressions } from "./suppressions.mjs";
import { APIPromise } from "../../core/api-promise.mjs";
import { DefaultFlatPagination, type DefaultFlatPaginationParams, PagePromise } from "../../core/pagination.mjs";
import { RequestOptions } from "../../internal/request-options.mjs";
/**
 * Named groups and group-scoped suppressions.
 */
export declare class EmailUnsubscribeGroups extends APIResource {
    suppressions: SuppressionsAPI.Suppressions;
    /**
     * Offset pagination only (`page[number]` default 1, `page[size]` default 25, max
     * 100). No `sort`/`filter`/cursor — ordering fixed `desc created_at, desc id`.
     * Uses the shared `QueryParser.parse_offset/1` — a malformed `page` (e.g. flat
     * `?page=1` instead of `?page[number]=1`) returns `400` (code `10015`), consistent
     * with `GET /v2/email_blocks`. `meta` includes `total_pages`.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const unsubscribeGroup of client.emailUnsubscribeGroups.list()) {
     *   // ...
     * }
     * ```
     */
    list(query?: EmailUnsubscribeGroupListParams | null | undefined, options?: RequestOptions): PagePromise<UnsubscribeGroupsDefaultFlatPagination, UnsubscribeGroup>;
    /**
     * Creates an account-owned unsubscribe group for associating email categories with
     * separate recipient suppression lists.
     *
     * @example
     * ```ts
     * const unsubscribeGroupResponse =
     *   await client.emailUnsubscribeGroups.create({
     *     name: 'Marketing Newsletter',
     *     description: 'Weekly product updates and promotions',
     *   });
     * ```
     */
    create(body: EmailUnsubscribeGroupCreateParams, options?: RequestOptions): APIPromise<UnsubscribeGroupResponse>;
    /**
     * If the group has 0 active suppressions, hard-deletes the row. With `force=true`,
     * soft-deletes all active suppressions first (status → `removed`, `group_id`
     * cleared, `removed` audit event per block) in a single transaction, then
     * hard-deletes the group. Without `force` and active suppressions present → `409`.
     * Audit trail is preserved. `force` only accepts the string `"true"` or boolean
     * `true`; all other values are false.
     *
     * @example
     * ```ts
     * await client.emailUnsubscribeGroups.delete(
     *   '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     * );
     * ```
     */
    delete(id: string, params?: EmailUnsubscribeGroupDeleteParams | null | undefined, options?: RequestOptions): APIPromise<void>;
    /**
     * Returns the account-owned unsubscribe group identified by ID.
     *
     * @example
     * ```ts
     * const unsubscribeGroupResponse =
     *   await client.emailUnsubscribeGroups.retrieve(
     *     '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     *   );
     * ```
     */
    retrieve(id: string, options?: RequestOptions): APIPromise<UnsubscribeGroupResponse>;
    /**
     * Partial update (only `name` / `description`). `PUT` is not routed.
     *
     * @example
     * ```ts
     * const unsubscribeGroupResponse =
     *   await client.emailUnsubscribeGroups.update(
     *     '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     *     {
     *       description: 'Weekly product updates and promotions',
     *     },
     *   );
     * ```
     */
    update(id: string, body: EmailUnsubscribeGroupUpdateParams, options?: RequestOptions): APIPromise<UnsubscribeGroupResponse>;
}
export type UnsubscribeGroupsDefaultFlatPagination = DefaultFlatPagination<UnsubscribeGroup>;
/**
 * Group list `meta` (consistent with `GET /v2/email_blocks`).
 */
export interface GroupListMeta {
    page_number: number;
    page_size: number;
    total_pages: number;
    total_results: number;
}
export interface UnsubscribeGroup {
    id: string;
    created_at: string;
    /**
     * Always present (not omit-nullable); `null` when unset.
     */
    description: string | null;
    name: string;
    /**
     * View-only.
     */
    record_type: 'email_unsubscribe_group';
    updated_at: string;
}
export interface UnsubscribeGroupResponse {
    data: UnsubscribeGroup;
}
export interface EmailUnsubscribeGroupListParams extends DefaultFlatPaginationParams {
}
export interface EmailUnsubscribeGroupCreateParams {
    name: string;
    description?: string | null;
}
export interface EmailUnsubscribeGroupDeleteParams {
    /**
     * Force-delete a group with active suppressions. Only `"true"` (string) or `true`
     * (bool) are truthy; all other values are false.
     */
    force?: 'true' | 'false' | boolean;
}
export interface EmailUnsubscribeGroupUpdateParams {
    description?: string | null;
    name?: string;
}
export declare namespace EmailUnsubscribeGroups {
    export { type GroupListMeta as GroupListMeta, type UnsubscribeGroup as UnsubscribeGroup, type UnsubscribeGroupResponse as UnsubscribeGroupResponse, type UnsubscribeGroupsDefaultFlatPagination as UnsubscribeGroupsDefaultFlatPagination, type EmailUnsubscribeGroupListParams as EmailUnsubscribeGroupListParams, type EmailUnsubscribeGroupCreateParams as EmailUnsubscribeGroupCreateParams, type EmailUnsubscribeGroupDeleteParams as EmailUnsubscribeGroupDeleteParams, type EmailUnsubscribeGroupUpdateParams as EmailUnsubscribeGroupUpdateParams, };
    export { Suppressions as Suppressions, type SuppressionListParams as SuppressionListParams, type SuppressionCreateParams as SuppressionCreateParams, type SuppressionDeleteParams as SuppressionDeleteParams, };
}
//# sourceMappingURL=email-unsubscribe-groups.d.mts.map