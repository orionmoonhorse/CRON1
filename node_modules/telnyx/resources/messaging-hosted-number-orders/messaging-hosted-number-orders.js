"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.MessagingHostedNumberOrders = void 0;
const tslib_1 = require("../../internal/tslib.js");
const resource_1 = require("../../core/resource.js");
const ActionsAPI = tslib_1.__importStar(require("./actions.js"));
const actions_1 = require("./actions.js");
const pagination_1 = require("../../core/pagination.js");
const path_1 = require("../../internal/utils/path.js");
/**
 * Manage your messaging hosted numbers
 */
class MessagingHostedNumberOrders extends resource_1.APIResource {
    constructor() {
        super(...arguments);
        this.actions = new ActionsAPI.Actions(this._client);
    }
    /**
     * Returns hosted-messaging orders for the authenticated account. Apply the
     * documented filters and pagination parameters to narrow the result set.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const messagingHostedNumberOrder of client.messagingHostedNumberOrders.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/messaging_hosted_number_orders', (pagination_1.DefaultFlatPagination), { query, ...options });
    }
    /**
     * Creates an order to enable Telnyx messaging on phone numbers whose voice service
     * remains with another carrier.
     *
     * @example
     * ```ts
     * const messagingHostedNumberOrder =
     *   await client.messagingHostedNumberOrders.create();
     * ```
     */
    create(body = {}, options) {
        return this._client.post('/messaging_hosted_number_orders', { body, ...options });
    }
    /**
     * Checks whether the supplied phone numbers are eligible for hosted messaging
     * before an order is created.
     *
     * @example
     * ```ts
     * const response =
     *   await client.messagingHostedNumberOrders.checkEligibility(
     *     { phone_numbers: ['string'] },
     *   );
     * ```
     */
    checkEligibility(body, options) {
        return this._client.post('/messaging_hosted_number_orders/eligibility_numbers_check', {
            body,
            ...options,
        });
    }
    /**
     * Delete a messaging hosted number order and all associated phone numbers.
     *
     * @example
     * ```ts
     * const messagingHostedNumberOrder =
     *   await client.messagingHostedNumberOrders.delete('id');
     * ```
     */
    delete(id, options) {
        return this._client.delete((0, path_1.path) `/messaging_hosted_number_orders/${id}`, options);
    }
    /**
     * Returns the current state, phone numbers, and required actions for the specified
     * hosted-messaging order.
     *
     * @example
     * ```ts
     * const messagingHostedNumberOrder =
     *   await client.messagingHostedNumberOrders.retrieve('id');
     * ```
     */
    retrieve(id, options) {
        return this._client.get((0, path_1.path) `/messaging_hosted_number_orders/${id}`, options);
    }
    /**
     * Validate the verification codes sent to the numbers of the hosted order. The
     * verification codes must be created in the verification codes endpoint.
     *
     * @example
     * ```ts
     * const response =
     *   await client.messagingHostedNumberOrders.validateCodes(
     *     'id',
     *     {
     *       verification_codes: [
     *         { phone_number: 'string', code: 'string' },
     *       ],
     *     },
     *   );
     * ```
     */
    validateCodes(id, body, options) {
        return this._client.post((0, path_1.path) `/messaging_hosted_number_orders/${id}/validation_codes`, {
            body,
            ...options,
        });
    }
    /**
     * Create verification codes to validate numbers of the hosted order. The
     * verification codes will be sent to the numbers of the hosted order.
     *
     * @example
     * ```ts
     * const response =
     *   await client.messagingHostedNumberOrders.createVerificationCodes(
     *     'id',
     *     {
     *       phone_numbers: ['string'],
     *       verification_method: 'sms',
     *     },
     *   );
     * ```
     */
    createVerificationCodes(id, body, options) {
        return this._client.post((0, path_1.path) `/messaging_hosted_number_orders/${id}/verification_codes`, {
            body,
            ...options,
        });
    }
}
exports.MessagingHostedNumberOrders = MessagingHostedNumberOrders;
MessagingHostedNumberOrders.Actions = actions_1.Actions;
//# sourceMappingURL=messaging-hosted-number-orders.js.map